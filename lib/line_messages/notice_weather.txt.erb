<% if @weather.today_is_rainy? %>
  <%= @date %>

  今日は雨が降ります！
  傘を持てば何とやら！

  --------------------
  時刻     天気 雨量
  <% @weather.forecast[:hourly][0...Weather::TAKE_WEATHER_HOUR].each do |hourly| %>
    <% hour_time  = Time.zone.at(hourly[:dt]).hour.to_s.rjust(2,'0') %>
    <% emoji_code = emoji(hourly) %>
    <% rain_fall  = hourly[:rain].present? ? hourly[:rain][:'1h'] : 0 %>

    <%= hour_time %>時 ･･･ <%= emoji_code %> <%= rain_fall.round %>[mm]
  <% end %>
  --------------------
<% else %>
  <% unless @line_user.silent_notice %>
    <%= @date %>

    今日は雨は降りません！
    良い一日をお過ごしください！

    --------------------
    時刻     天気 雨量
    <%  @weather.forecast[:hourly][0...Weather::TAKE_WEATHER_HOUR].each do |hourly| %>
      <% hour_time  = Time.zone.at(hourly[:dt]).hour.to_s.rjust(2,'0') %>
      <% emoji_code = emoji(hourly) %>
      <% rain_fall  = hourly[:rain].present? ? hourly[:rain][:'1h'] : 0 %>

      <%= hour_time %>時 ･･･ <%= emoji_code %> <%= rain_fall.round %>[mm]
    <% end %>
    --------------------
  <% end %>
<% end %>
